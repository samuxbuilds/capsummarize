# Environment Configuration for CapSummarize Browser Extension

# Node environment (development/production)
# Set to 'development' for debug logging, 'production' for release builds
NODE_ENV=development
